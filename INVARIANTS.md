# INVARIANTS — กฎที่ห้ามพัง

เอกสารนี้คือ **สัญญาความถูกต้อง** ของระบบติดตามแผนผลิต (production_plan_tracker.html)
ทุก PR ต้องถูกตรวจว่าไม่ละเมิดข้อใดข้อหนึ่งในนี้ ก่อนเสนอให้เจ้าของอนุมัติ

> ถ้าการแก้ปัญหาหนึ่งจำเป็นต้องละเมิดข้อใดข้อหนึ่ง **ห้ามแก้เอง** — ให้หยุด
> แล้วเขียนใน PR ว่าติดข้อไหน และเสนอทางเลือกให้เจ้าของตัดสิน

---

## A. ความถูกต้องของยอด

**A1 — ยอดสะสมนับเฉพาะรายการที่ยังไม่ถูกยกเลิก**
ทุกฟังก์ชันที่รวมยอด (`buildCumMap` · `buildCumSplitMap` · `buildRecordIndex`)
ต้อง `continue` ทันทีเมื่อ `r.voided`
การเพิ่มฟังก์ชันรวมยอดใหม่โดยลืมเช็ก `voided` จะทำให้ยอดที่ยกเลิกไปแล้วกลับมานับซ้ำ

**A2 — หนึ่ง order + หนึ่ง process + หนึ่งวัน = หนึ่ง record**
กุญแจประจำตัวของ record คือ `orderId|process|date`
ทั้ง `saveEntryValue()` และ `computeMergePlan()` ใช้กุญแจนี้ในการจับคู่
**ถ้าเปลี่ยนกุญแจนี้ การรวมข้อมูลจากไฟล์และการซิงค์จะสร้างรายการซ้ำทันที**

**A3 — ลำดับขั้นตอนคงที่**
`winding → assembly → support → inspection → shipping` กำหนดใน `PROCESSES` และ `PREV_PROCESS`
(ขั้น `support` เพิ่มเข้ามาเมื่อ 2026-08-14 ตามการอนุมัติของเจ้าของใน issue #17)
การเพิ่ม/ลบ/สลับขั้นตอนกระทบทั้ง `emptyCum()` · `computeDeadlines()` · `computeStatus()`
· `buildCumSplitMap()` · ตาราง Dashboard · การส่งออก Excel — ต้องแก้ให้ครบทุกจุดพร้อมกัน

**A4 — deadline คำนวณจาก `orderDate` ของแต่ละใบ ไม่ใช่ค่าคงที่**
`computeDeadlines()` = `orderDate + deadlineOffsets[stage]`
`deadlineOffsets` ตั้งค่าได้จากหน้า Settings และ **ซิงค์ข้ามเครื่อง**
ห้าม hardcode ตัวเลข 10/17/24/28 ซ้ำที่อื่น ให้อ่านจาก `state.deadlineOffsets` เสมอ
`deadlineOffsets[stage] === null` = "ยังไม่กำหนด" → deadline ของขั้นนั้นเป็น `null`
และห้ามตัดสินว่าล่าช้า (ปัจจุบันคือ `support` ที่รอเจ้าของใส่เลขวันเอง) ห้ามแทนด้วย 0

**A5 — `computeStatus` เรียงลำดับการตัดสินจากปลายน้ำขึ้นต้นน้ำ**
ส่งของครบ → เกินกำหนดส่งของ → ล่าช้า Inspection → Support → Assembly → Winding → ใกล้ครบกำหนด → ปกติ
ลำดับนี้จงใจ เพื่อให้รายงานสถานะที่ "แย่ที่สุด" ก่อน ห้ามสลับลำดับ `if`

---

## B. ร่องรอยตรวจสอบ (audit trail)

**B1 — ห้ามลบ record ออกจาก `state.records`**
การลบคือ `voided = true` + อัปเดต `updatedAt` + `_dirty = true`
**เหตุผลที่ลบจริงไม่ได้:** ถ้าลบออกจาก array แต่เซิร์ฟเวอร์ยังมีอยู่
การซิงค์รอบถัดไปจะดึงกลับมาใหม่ ผู้ใช้จะเห็นว่า "ลบไม่ออก"
อ้างอิง: คอมเมนต์เหนือ `buildCumMap()`

**B2 — เคลียร์ช่องกรอกให้ว่าง ต้องไม่ลบข้อมูล**
`saveEntryValue()` จงใจ `return` ทันทีเมื่อค่าว่าง
ถ้าผู้ใช้ต้องการแก้ยอดที่กรอกผิด ต้องพิมพ์ `0` ทับ
ห้ามเปลี่ยนให้ช่องว่าง = ลบ (ด้วยเหตุผลเดียวกับ B1)

**B3 — พิมพ์ค่าใหม่ทับรายการที่ยกเลิกไปแล้ว = กู้คืนรายการนั้น**
`saveEntryValue()` ตั้ง `voided = false` เมื่อเขียนทับ record เดิม — เป็นพฤติกรรมที่ตั้งใจ

**B4 — `id` และ `createdAt` ห้ามแก้หลังสร้าง**

---

## C. วันที่ — จุดที่เคยพังมาแล้ว

**C1 — แยกให้ชัดระหว่าง "วันปฏิทินล้วน" กับ "timestamp"**

| ประเภท | ฟิลด์ | รูปแบบ |
|---|---|---|
| วันปฏิทินล้วน | `orderDate` `planWinding` `planAssembly` `planSupport` `planInspection` `date` | `YYYY-MM-DD` |
| timestamp | `createdAt` `updatedAt` `importedAt` | ISO 8601 เต็ม |

**ห้ามเอา `createdAt`/`updatedAt`/`importedAt` ไปใส่ใน `ORDER_DATE_FIELDS` หรือ `RECORD_DATE_FIELDS`**
เพราะ `normalizeDateOnly()` จะตัดส่วนเวลาทิ้ง แล้วการเทียบ "ใครแก้ทีหลัง" จะพังทั้งระบบ

**C2 — ข้อมูลที่มาจากภายนอกต้อง normalize ก่อนใช้เสมอ**
ทั้งจาก Google Sheets และจากไฟล์ที่ import — เรียก `normalizeRowDates()` ก่อนเทียบหรือเก็บ
เคยมีบั๊กที่ Google Sheets คืนวันที่มาคนละรูปแบบ ทำให้กุญแจ `orderId|process|date` ไม่ตรง
และเกิดรายการซ้ำ — `migrateCorruptedDates()` คือตัวซ่อมข้อมูลที่เพี้ยนไปแล้ว **ห้ามลบทิ้ง**

**C3 — เทียบวันด้วย string ของ `YYYY-MM-DD` ได้ (เช่น `r.date <= dl`)**
ใช้ได้เพราะรูปแบบนี้เรียงตามตัวอักษรได้ ถ้าเปลี่ยนเป็น `DD/MM/YYYY` การเทียบทั้งระบบจะพังเงียบ ๆ
`fmtDateTH()` แปลงเป็น `DD/MM/YYYY` **สำหรับแสดงผลเท่านั้น** ห้ามเอาค่าที่แปลงแล้วไปเก็บหรือไปเทียบ

---

## D. การซิงค์กับ Google Sheets

**D1 — `Content-Type` ต้องเป็น `text/plain;charset=utf-8`**
ไม่ใช่ `application/json` เพราะจะทำให้เบราว์เซอร์ยิง CORS preflight (OPTIONS)
ซึ่ง **Apps Script Web App ไม่รองรับ** และ sync จะพังทั้งระบบ
อ้างอิง: `gsApi()` — มีคอมเมนต์กำกับไว้แล้ว

**D2 — `_dirty` ห้ามส่งขึ้นเซิร์ฟเวอร์** ต้องถอดด้วย `cleanForPush()` ก่อนเสมอ

**D3 — ของที่ยังไม่ได้ส่ง ห้ามถูกทับ**
ตอน merge ข้อมูลที่ pull มา ถ้า `local._dirty === true` ให้ `continue` ทันที
ไม่ว่า `updatedAt` ฝั่งเซิร์ฟเวอร์จะใหม่กว่าแค่ไหน
**ละเมิดข้อนี้ = ยอดที่พนักงานคีย์ไว้หายโดยไม่มีใครรู้**

**D4 — หลัง push สำเร็จ ต้องเขียน `updatedAt` ทับด้วย `serverTime`**
ถ้านาฬิกาเครื่องเดินเร็วกว่าเซิร์ฟเวอร์ การแก้ไขจากเครื่องอื่นจะถูกมองข้ามตลอดไป

**D5 — `_dirty === undefined` ต้องถือว่า "ยังไม่เคยส่ง"**
ข้อมูลที่บันทึกไว้ก่อนมีฟีเจอร์ sync ไม่มี flag นี้เลย
ตอนโหลด state จึงตั้งเป็น `true` เพื่อให้ครั้งแรกที่ต่อ Google Sheets อัปโหลดของเก่าขึ้นไปครบ
ห้ามเปลี่ยนเป็น `false` เพราะข้อมูลเก่าจะไม่ถูกส่งขึ้นเลย

**D6 — `chartPref` เป็นค่าเฉพาะเครื่อง ห้ามซิงค์**
เป็นการตั้งค่ามุมมองกราฟของแต่ละคน ไม่ใช่ข้อมูลร่วม

**D7 — เขียนลงเครื่องก่อนเสมอ (offline-first)**
ทุกการบันทึกต้อง `saveState()` ให้สำเร็จก่อน แล้วค่อย `doSync()`
ห้ามทำให้การบันทึกขึ้นกับผลของ network

---

## E. การเก็บข้อมูลในเครื่อง

**E1 — key ห้ามเปลี่ยนโดยไม่มี migration**
`tue_order_tracker_v1` · `tue_order_tracker_sync_v1`
ข้อมูลของพนักงานอยู่ในเครื่องเขา **เปลี่ยน key = ข้อมูลหายและกู้ไม่ได้**

**E2 — `loadState()` ต้องอ่านข้อมูลเวอร์ชันเก่าได้เสมอ**
รูปแบบที่ใช้อยู่คือ `Object.assign(defaultState(), parsed, { ...ที่ต้องระวังเป็นพิเศษ })`
การเพิ่ม field ใหม่ต้องใส่ค่า default ใน `defaultState()` ด้วยเสมอ
เปลี่ยนชื่อ / ลบ / เปลี่ยนชนิดของ field เดิม ต้องเขียน migration
(ดู `migrateCorruptedDates()` เป็นตัวอย่าง) และต้องระบุใน PR ว่า **"PR นี้แตะโครงสร้างข้อมูล"**

**E3 — `loadState()` ต้องไม่โยน exception**
ถ้า JSON เสีย ต้อง `catch` แล้วคืน `defaultState()` ไม่ใช่ปล่อยให้แอปเปิดไม่ขึ้น

---

## F. การ deploy

**F1 — push ขึ้น `main` = ขึ้น production ทันที**
GitHub Pages เผยแพร่ให้อัตโนมัติ ไม่มี staging ไม่มี build step

**F2 — ต้องเป็นไฟล์เดียว และต้องทำงานได้โดยไม่ต้องต่อเน็ต**
ExcelJS ถูก vendor ไว้ในไฟล์แล้ว (บรรทัดยาว ๆ ช่วงกลางไฟล์)
**ห้ามเปลี่ยนไปโหลดจาก CDN** และ **ห้ามแตะบรรทัด vendor เหล่านั้น**

> ข้อยกเว้นเดียว — การเปลี่ยนหรือเพิ่มไลบรารีที่ฝังไว้ ซึ่งเป็นเรื่องที่นาน ๆ ทีและตั้งใจทำ
> **เจ้าของต้องติด label ชื่อ `vendor-change` ที่ PR ใบนั้นด้วยตัวเอง** และเขียนในหัว PR
> ว่าเปลี่ยนอะไรเป็นอะไรเพราะอะไร · PR ของ agent แตะบรรทัด vendor ไม่ได้เลยแม้จะติด label
>
> ที่ใช้ label ไม่ใช่ข้อความในหัว PR เพราะ agent เขียนหัว PR ของตัวเองอยู่แล้วระหว่างเปิดใบ
> การติด label เป็นการกระทำแยกต่างหากที่มีชื่อคนทำและเวลาอยู่ในไทม์ไลน์
> **ยังไม่ใช่ด่านที่หัก agent ซึ่งถือ credential ของเจ้าของได้** ด่านสุดท้ายจริงคือสายตาเจ้าของ
ห้ามเพิ่ม build step ห้ามเพิ่ม dependency ที่ตัวแอปต้องใช้ตอนรัน
(`package.json` มีไว้ให้ชุดเทสเท่านั้น ไม่ได้ถูกโหลดตอนแอปทำงาน)

**F3 — repo นี้เป็น public**
ห้าม commit ข้อมูลธุรกิจ (ไฟล์แผนผลิต ยอดจริง ชื่อลูกค้า)
**ห้าม hardcode URL ของ Apps Script หรือ token** — ผู้ใช้ต้องกรอกเองเสมอ

**F4 — แก้ไฟล์แอปเมื่อไหร่ ต้องบัมป์ `<meta name="app-version">` เสมอ**
รูปแบบ `YYYY-MM-DD.ลำดับของวันนั้น` ประกาศที่ `<head>` ที่เดียว โค้ดอ่านจากตรงนั้น

เหตุผลไม่ใช่เรื่องความเป็นระเบียบ แต่เพราะเครื่องในโรงงานเปิดโปรแกรมค้างไว้ทั้งวัน
และบางเครื่องเก็บไฟล์เก่าไว้ไม่ยอมโหลดใหม่ **เลขรุ่นคือสิ่งเดียวที่ใช้ตัดสินได้ว่า
อาการที่แจ้งมาเป็นบั๊กจริง หรือแค่คนละรุ่นกัน** — เคยเสียเวลาไปครึ่งวันกับเรื่องนี้มาแล้ว

> ⚠️ **เลขรุ่นที่โกหกอันตรายกว่าไม่มีเลขเลย** เพราะทุกคนจะเชื่อมัน
> กฎข้อนี้จึงต้องบังคับด้วยเครื่อง ไม่ใช่ความจำ — CI มี job `ตรวจว่าบัมป์ app-version แล้ว`
> ที่จะทำให้ PR แดงถ้าแตะไฟล์ `.html` แล้วไม่ได้บัมป์

---

## G. ประสบการณ์ผู้ใช้ที่ห้ามเสีย

**G1 — ภาษาไทยทั้งหมด** ข้อความที่ผู้ใช้เห็นต้องเป็นภาษาไทย รวมถึงข้อความ error

**G2 — หน้าคีย์ยอดต้องคีย์รัว ๆ ได้**
บันทึกอัตโนมัติเมื่อออกจากช่อง ห้ามเพิ่มปุ่ม "บันทึก" ที่บังคับให้ต้องกด
ห้ามทำลายลำดับ tab หรือ `pendingFocusOrderId` ที่มีอยู่

**G3 — ห้ามใส่ค่าจากผู้ใช้ลง `innerHTML` โดยไม่ผ่าน `escapeHtml()`**
โค้ดเดิมใช้ `escapeHtml()` อยู่แล้วในทุกจุดที่ประกอบ HTML เอง — ต้องรักษาไว้
