# CLAUDE.md — คู่มือสำหรับ AI agent ที่เข้ามาแก้ repo นี้

**อ่าน [INVARIANTS.md](INVARIANTS.md) ให้จบก่อนแตะโค้ดทุกครั้ง** ไฟล์นั้นคือกฎที่ห้ามพัง
ไฟล์นี้คือแผนที่ว่าอะไรอยู่ตรงไหน และทำงานยังไงให้ปลอดภัย

---

## 1. ระบบนี้คืออะไร

ระบบติดตามความคืบหน้าการผลิตตามใบสั่งซื้อ ใช้แทนกระดานไวท์บอร์ด/Excel
ผู้ใช้คือฝ่ายวางแผนและหัวหน้าไลน์ผลิต ใช้บนคอมในโรงงาน ภาษาไทยล้วน

งานหลักที่ระบบทำ:
- **นำเข้าแผนงาน** จากไฟล์ Excel ของลูกค้า
- **คีย์ยอดรายวัน** ว่าแต่ละใบสั่งซื้อเดินไปถึงไหน — หน้านี้มีปุ่มครบทุกขั้นใน `DEFAULT_PROCESSES`
- **ใบส่งสินค้า** คีย์จำนวนที่ส่งราย PO และกรอกฟอร์ม FM-ST-07 ให้
- **เทียบยอดกับไฟล์ Call In ของ Delta** อ่านอย่างเดียว ดูว่ายอดค้างส่งของเราตรงกับที่ Delta บันทึกไว้มั้ย
  และ**เก็บยอดของ Delta ไว้ใช้เป็นเลข Wip bal. บนใบส่งสินค้า**

> ⚠️ **`google-apps-script.gs` อยู่ใน repo แล้วตั้งแต่ 1 ก.ย. 2026** — ยังต้องก๊อปไป deploy ด้วยมือเหมือนเดิม
> แต่ตอนนี้ `tests/sync-contract.spec.js` **อ่านทั้งไฟล์นี้กับตัวแอปมาเทียบคอลัมน์กันทุกครั้งที่รันเทส**
> เพิ่มคอลัมน์ฝั่งไหนแล้วลืมอีกฝั่ง เทสจะแดงพร้อมบอกชื่อคอลัมน์ · ไฟล์นี้ไม่มี token/URL จริง
>
> ⚠️ **สามจำนวนนี้ไม่ใช่อันเดียวกัน** — `FG คงเหลือ` = Σ inspection − Σ shipping (ของในคลัง) ·
> `ยอดค้างส่งของเรา` = Σ (orderQty − shipped) · `ยอดค้างส่งของ Delta` = Σ deltaWip
> **คู่ที่เทียบกันได้คือสองอันหลัง** — FG ไม่รวมของที่ยังไม่ได้ผลิต ส่วนยอดค้างส่งรวม
> เอามาเทียบผิดคู่แล้วจะสรุปว่า "ยอดไม่ตรง" ทั้งที่ทั้งสองฝั่งถูก
>
> ⚠️ **ยอดค้างส่งมีสองเลข ห้ามเอามาแทนกัน** — `wipBalance()` คือยอดตามบัญชีเรา
> ใช้ตัดสินว่าจะโชว์ PO ไหนบนหน้าใบส่งสินค้า · `deltaWipOf()` คือยอดตามบัญชี Delta
> ใช้เป็นเลขที่พิมพ์ลงกระดาษเท่านั้น · **สองเลขนี้ไม่ตรงกันได้ และนั่นคือข้อมูล ไม่ใช่บั๊ก**
> ถ้าเอายอดของ Delta ไปใช้กับตัวกรองบนจอ PO ที่ไม่มีในไฟล์จะหายไปจนส่งของใบนั้นไม่ได้เลย
>
> ⚠️ **ยอดของ Delta นับเฉพาะงวดล่าสุด** — `deltaWipOf()` คืนเฉพาะแถวของ `currentDeltaWeek()`
> Delta ไม่ใส่ 0 ให้ใบที่ส่งครบแล้ว แต่ตัดใบนั้นออกจากไฟล์งวดใหม่ไปเลย แถวของงวดก่อนจึงค้างเป็นประวัติ
> ถ้าหยิบโดยไม่ดูงวด ยอดเก่าจะค้างตลอดไปทั้งหน้า FG หน้าใบส่งสินค้า และกระดาษ (เจ้าของเจอ 14 ก.ย. 2026)
> ผลข้างเคียง: นำเข้าชีตแรกของงวดใหม่แล้วยังไม่นำเข้าชีตที่เหลือ ใบในชีตที่เหลือจะนับว่า "ไม่อยู่ในไฟล์" จนกว่าจะนำเข้าครบ
>
> ⚠️ **หน้ายอดคงคลัง FG หักยอดที่เราส่งแล้วแต่ Delta ยังไม่นับ** (เจ้าของเลือก 15 ก.ย. 2026 · `deltaWipLeft()`)
> Delta นับส่งแล้ว = ยอดสั่ง − Wip bal. · ส่วนที่เราส่งเกินจากนั้นหักออก · ครอบที่ 0 · ไม่ใช้วันที่
> ช่อง "ต่าง" จึงไม่บอกว่า Delta บันทึกช้ากว่าเรา แต่ยังบอกว่า Delta นับส่งมากกว่าที่เราคีย์ · ถือว่ายอดสั่งเท่ากับ PO QTY ของ Delta
> (#87 เคยหักตามวันนำเข้าไฟล์ — ใช้ไม่ได้จริง ยอดที่ส่งก่อนวันนำเข้าแต่ Delta ยังไม่บันทึก ไม่ถูกหักเลย)
> **หน้าใบส่งสินค้าและช่อง Wip bal. บนกระดาษไม่หัก** — ยังเป็นยอดของ Delta ตรง ๆ (14 ก.ย. เคยให้หักทุกที่ แล้วเจ้าของเปลี่ยนเป็นเฉพาะหน้า FG)
>
> ⚠️ **ตัวแก้มือ Wip bal. ของ Delta** (เจ้าของสั่ง 15 ก.ย. 2026 · `editDeltaWip()` · คอลัมน์ `wipOverride` `overrideNote` `overrideBy` `overrideAt`)
> Delta ทำยอดในไฟล์ผิดได้ · ยอดที่แก้ **ชนะยอดในไฟล์ทุกที่ รวมกระดาษ** ผ่าน `deltaWipNum()` จุดเดียว · หน้า FG หักส่วนที่ Delta ยังไม่นับต่อจากยอดที่แก้
> อยู่บนแถวของงวดนั้น จึงหมดอายุเองเมื่อนำเข้างวดใหม่ · ยอดในไฟล์ (`wip`) ไม่ถูกทับ ดูได้จาก `deltaWipFileNum()`
> บังคับเหตุผล + ชื่อผู้แก้ทุกครั้งรวมตอนยกเลิก · **ยกเลิกต้องเขียน `wipOverride: ''` ห้ามลบฟิลด์** — Apps Script คงค่าเดิมถ้าฟิลด์หาย (`KEEP_IF_ABSENT` กันเครื่องรุ่นเก่าล้างยอดที่แก้)
- **ยอดคงคลัง FG** ของที่ผ่าน Inspection แล้วแต่ยังไม่ส่ง · แยกตาม P/N + หน่วย · เทียบยอดค้างกับ Delta
- **Dashboard** แถบความคืบหน้าทุกขั้นรวม Shipping · Aging · สถานะล่าช้า/ใกล้ครบกำหนด · กราฟ
  (คอลัมน์ Shipping เคยเป็น FG คงเหลือ 2–12 ก.ย. 2026 · เจ้าของสั่งกลับเป็นยอดส่ง · FG ดูที่หน้ายอดคงคลัง FG)
- **รวมข้อมูลจากไฟล์** ที่ export มาจากเครื่องอื่น (มีหน้าจอสรุปก่อนรวม)
- **ออกรายงาน Excel**
- **ซิงค์หลายเครื่อง** ผ่าน Google Sheets (ไม่บังคับ ใช้ offline ได้)

> ⚠️ **ตัวกรอกฟอร์ม Call In ถูกถอดออกเมื่อ 1 ก.ย. 2026** — เจ้าของแจ้งว่าสร้างมาจากความเข้าใจผิด ไม่ได้ใช้
> เหลือเฉพาะตัวอ่านกับตัวเทียบยอด ซึ่งกำลังจะกลายเป็นแหล่งของ Wip bal. บนใบส่งสินค้า
> **ห้ามใส่ตัวเขียนไฟล์กลับมาโดยไม่ถามเจ้าของก่อน** — มีเทสคุมไว้แล้ว

### โครงไฟล์ Call In ตัวจริง — ตรวจเมื่อ 5 ก.ย. 2026

เปิดไฟล์ของ WK 35 ดูจริง ๆ แล้วพบว่าความเข้าใจเดิมสองข้อผิดทั้งคู่:

| ชีต | แถวที่มี PO | หน่วยที่แยกได้จากรหัส PO | P/N | PO NO | PO QTY | Wip bal. |
|---|---|---|---|---|---|---|
| ชีตแรก | 348 | TUE-H 342 · แยกไม่ได้ 6 (`TM####T###`) | A | B | **E** | **F** |
| ชีตที่สอง | 60 | TUE-U 59 · **TUE-H ปนมา 1** | A | B | **D** | **E** |

> ⚠️ **ชีตไม่ได้แบ่งตามหน่วย** — แต่ละชีต "ส่วนใหญ่" เป็นหน่วยเดียว ไม่ใช่ "ทั้งหมด"
> ตรรกะอะไรก็ตามที่ตั้งอยู่บน "หนึ่งชีต = หนึ่งหน่วย" จะพังกับไฟล์จริงทันที
> เคยจะเขียน `saveDeltaWip` ให้ล้างยอดทั้งหน่วย แล้วใบ TUE-H ที่ปนอยู่ในชีตที่สองใบเดียว
> จะพายอดของ TUE-H อีก 341 ใบหายไปด้วย · **ขอบเขตต้องเป็น "ต่อใบ" เท่านั้น**
> ซึ่งยังทนกับรหัส PO ที่ `subNameFromPo()` แยกไม่ออกด้วย (ไฟล์จริงมี 6 แถว)

> ⚠️ **สองชีตวางคอลัมน์ไม่ตรงกัน** — ชีตแรกมี `New SAP` แทรกอยู่ที่ D ดัน PO QTY กับ Wip bal.
> ไปทางขวาหนึ่งช่อง การยึดตัวอักษรคอลัมน์ตายตัวจึงอ่านผิดชีตใดชีตหนึ่งเสมอ
> **แก้แล้ว 6 ก.ย. 2026** — `readCallInSheet()` หา P/N · PO No. · PO QTY · Wip bal.
> จากข้อความหัวตารางทั้งหมดผ่าน `CALLIN_HEAD` ไม่เหลือ `CALLIN_COL` ที่ยึดตำแหน่งแล้ว
> แถวหัวตารางก็หาจากข้อความ `P/N` ที่ช่องไหนก็ได้ ไม่ใช่คอลัมน์ A เสมอ
> ขาดหัวไหนขึ้นข้อความบอกชื่อหัวนั้นตรง ๆ ไม่เดาคอลัมน์ให้

> ⚠️ **ช่อง Wip bal. ว่างได้** — ชีตแรกมี 39 ใบที่เว้นว่าง แปลว่า "เขายังเรียกใบนี้อยู่
> แต่ไม่ได้บอกว่าค้างเท่าไหร่" ไม่ใช่ "ค้างศูนย์" และไม่ใช่ "ไม่อยู่ในไฟล์"
> เก็บเป็น `wip: null` · **ห้ามทิ้งแถว** เพราะ "มีแถว" คือหลักฐานว่าใบนี้อยู่ในไฟล์
> ของเดิมทิ้งแถว ยอดที่ผิดจากการอัปครั้งก่อนจึงรอดมาตลอด อัปทับกี่ครั้งก็ไม่หาย (#62)

> ⚠️ **จอใบส่งงานแสดงเฉพาะใบที่อยู่ในไฟล์ Call In ของงวดล่าสุด** (เจ้าของสั่ง 5 ก.ย. 2026)
> ตัดสินด้วย `inCallIn` = มีแถวที่ยังไม่ยกเลิก **และ** งวดของแถวตรงกับ `currentDeltaWeek()`
> ถ้าเทียบแค่ "มีแถว" ใบที่ Delta ตัดออกแล้วจะยังโชว์พร้อมยอดของสัปดาห์ที่แล้ว
> เพราะเราเก็บประวัติไว้ ไม่ได้ลบแถวเก่าทิ้ง
>
> `currentDeltaWeek()` ดู `createdAt` ของแถวที่สร้างล่าสุด ไม่ใช่เลข `week` ที่มากที่สุด
> (ปลายปี wk52 → wk01 เลขน้อยลงแต่ใหม่กว่า) และเทียบด้วย **"งวด"** ไม่ใช่ "ครั้งที่นำเข้า"
> เพราะหนึ่งงวดมีหลายชีต ถ้าเทียบเป็นครั้ง นำเข้าชีตที่สองแล้วชีตแรกหายจากจอทั้งชุด
>
> ยังเป็นชั้นแสดงผลล้วน ๆ — `deliveryGroups()` ไม่ตัดแถวทิ้ง และปลดติ๊กก็เห็นครบเหมือนเดิม
> ถ้ายังไม่ได้นำเข้าไฟล์เลย จะไม่ซ่อนอะไรทั้งสิ้น ไม่งั้นทั้งหน่วยส่งของไม่ได้
> ยอดสรุปบอกงวดที่กำลังยึดอยู่ เพราะการกดเก็บไฟล์งวดเก่าจะดึงทั้งกระดานไปงวดนั้น

> ⚠️ **ใบสั่งยกเลิกได้ ลบไม่ได้** (5 ก.ย. 2026) — `order.voided` + `activeOrders()`
> พนักงานแจ้งว่านำเข้าแผนผิดแล้วแก้ไม่ได้ ทั้งกรณีพิมพ์รหัส PO ผิด และกรณีลูกค้ายกเลิก PO ทีหลัง
> (ยอดผิดเฉย ๆ ไม่ต้องยกเลิก — นำเข้าไฟล์ที่แก้แล้วทับได้ ระบบจับคู่ด้วย `PO|P/N` แล้วอัปเดตให้)
>
> **ลบออกจาก `state.orders` ไม่ได้เด็ดขาด** รอบซิงค์ถัดไปจะดึงกลับมาจากเซิร์ฟเวอร์ (B1)
> และยอดที่คีย์ไว้ทุกรายการอ้าง `orderId` อยู่ ใบหายจริงเมื่อไหร่ยอดกลายเป็นกำพร้า
>
> **`activeOrders()` ใช้เฉพาะที่ "ไล่รายการใบสั่ง"** — Dashboard · บันทึกยอดรายวัน · ใบส่งงาน ·
> FG · ตัวเลือกในตัวกรอง · รายงาน Excel · การเก็บยอด Call In
> ส่วนที่ค้นใบจาก `orderId` ของยอดที่คีย์ไว้ (พวก `orderById`) **ต้องเห็นใบที่ยกเลิกด้วย**
> ไม่งั้นหน้าแก้ไข/ลบรายการจะโชว์ PO เป็น `?` แล้วตามไม่ได้ว่ายอดนั้นของใบไหน
>
> นำเข้าไฟล์แผนทับ **ไม่ปลุก** ใบที่ยกเลิกแล้ว เพราะ `Object.assign` ทับเฉพาะฟิลด์ที่มาจากไฟล์
> ซึ่งไม่มี `voided` · ลูกค้าเปิด PO นั้นใหม่ต้องกดปุ่มกู้คืนเอง
>
> **ต้องแปะ `google-apps-script.gs` แล้ว redeploy** — `ORDER_COLS` มีคอลัมน์ `voided` เพิ่ม
> ถ้ายังไม่ทำ การยกเลิกจะอยู่แค่ในเครื่องที่กด เครื่องอื่นยังเห็น PO นั้นอยู่
> คอลัมน์ใหม่ **ต้องต่อท้ายเสมอ** เพราะ `doPushRows` เขียนด้วยตำแหน่ง แต่ `sheetOf()` เติมหัวตารางต่อท้าย

> ⚠️ **ยอดส่งของนำเข้าจากไฟล์ไม่ได้แล้ว** ตั้งแต่ 30 ส.ค. 2026 (PR #34)
> การนำเข้าจากไฟล์ "ใบส่งงาน TPP-UNION" ถูกตัดออก เพราะเขียนกุญแจ `orderId|shipping|date`
> ชนกับหน้าใบส่งสินค้า จึงทับกันเงียบ ๆ โดยไม่มีใครรู้ว่าอันไหนถูก
> ยอดที่เคยนำเข้าไว้ยังอยู่ครบและยังบอกที่มาได้
>
> **แต่ยังเหลือสองทางที่เขียนกุญแจเดียวกันได้** — หน้าใบส่งสินค้า (`saveAllocation()`)
> คือทางที่ควรใช้ ส่วนแท็บ `🚚 ส่งของ` ในหน้าคีย์ยอดรายวัน **ยังเปิดอยู่โดยตั้งใจ**
> เป็นทางสำรองระหว่างเปลี่ยนผ่าน · ทั้งสองทางทับกันได้ตาม A2 ระวังเวลาแก้ฝั่งใดฝั่งหนึ่ง
> (การปิดทางคีย์มือเป็นงานที่เลื่อนไว้ ยังไม่ได้ทำ)

**ขั้นตอนการผลิต** ห้าขั้นตั้งต้นอยู่ใน `DEFAULT_PROCESSES` (INVARIANTS A3) · `winding` → `assembly` → `support` → `inspection` → `shipping`
เจ้าของสั่งเมื่อ 12 ก.ย. 2026 ให้เพิ่มขั้นได้จากในโปรแกรม — แผง "ขั้นตอนการผลิต" ในหน้าข้อมูล & แชร์
เพิ่ม · เปลี่ยนชื่อ · ซ่อน ได้เมื่อต่อ Google Sheets และใส่รหัสหัวหน้า · `inspection` กับ `shipping` ล็อกอยู่ท้ายสุด · ลบขั้นไม่ได้
รหัสหัวหน้าเก็บเป็น SHA-256 + salt ใน Meta `processAdmin` — **เป็นรั้วกันกดพลาด ไม่ใช่ความปลอดภัยจริง** (ใครดึงค่าตั้งได้ก็เห็น hash)

> ⚠️ **ขั้นนับแยก (`standalone`) — เจ้าของสั่ง 20 ก.ย. 2026** · ขั้นที่ยอดไม่เกี่ยวกับสายหลักเลย
> (งานซ่อม งานนอกแผน) คีย์ยอดเก็บไว้ดูย้อนหลังได้ แต่ไม่ได้ไหลมาจากขั้นไหนและไม่ไหลไปขั้นไหน
> **ที่ไหนที่แปลว่า "สายการผลิต" ต้องอ่าน `MAIN_PROCESSES`** ไม่ใช่ `PROCESSES` (INVARIANTS A3 · A5)
> ไม่ต้อง redeploy Apps Script — `processes` เก็บเป็น JSON ก้อนเดียวใน Meta และ `processListError()` ไม่ตัดฟิลด์ที่ไม่รู้จักทิ้ง
> สร้างได้จากแผง "ขั้นตอนการผลิต" — ติ๊ก **"นับแยก ไม่อยู่ในสายการผลิต"** ตอนกดเพิ่มขั้น
> **ชนิดของขั้นเปลี่ยนทีหลังไม่ได้** มีแต่เปลี่ยนชื่อกับซ่อน (เหมือน `id`) · ห้าขั้นตั้งต้นเป็นสายหลักเสมอ
> ตารางจัดการขั้นมีป้าย `นับแยก` บอกไว้ · ซ่อนขั้นสายหลักจนเหลือแต่ขั้นนับแยกไม่ได้
> ขั้นนับแยกแทรกเป็นขั้นแรกได้ แต่ปุ่มตั้งต้นของหน้าคีย์ยอดยังเป็นขั้นแรกของ `MAIN_PROCESSES` เสมอ

> 🔒 **เครื่องรุ่นเก่าลบธง `standalone` ได้ — เจ้าของตัดสินแล้วว่าไม่ทำด่านกัน (20 ก.ย. 2026)**
> **อย่าเปิดประเด็นนี้ใหม่แล้วเสนอ redeploy อีก** เว้นแต่เจ้าของสั่งเอง
>
> อาการ: `storedProcessList()` รุ่นก่อน 20 ก.ย. 2026 ไม่พก `standalone` ไปด้วย
> เครื่องที่ค้างหน้ารุ่นนั้นไว้แล้วกดเพิ่ม/เปลี่ยนชื่อ/ซ่อนขั้น จะ push รายการที่ไม่มีธงขึ้นไปทับ
> ธงหายจากทุกเครื่อง แล้วขั้นนั้นกลับเข้าสายหลัก → **ทุกใบขึ้นล่าช้า**
> ฝั่งเซิร์ฟเวอร์ไม่มีอะไรกัน — `processListError()` กันแค่ "ลบขั้นไม่ได้" ไม่ได้กัน "ลบฟิลด์ไม่ได้"
> (ต่างจากตาราง DeltaWip ที่มี `KEEP_IF_ABSENT` คุมรูปแบบเดียวกันนี้ไว้แล้ว)
>
> **ทำไมถึงไม่ทำด่าน:** ต้องครบสามอย่างพร้อมกันถึงจะเกิด — รู้รหัสหัวหน้า **และ** เครื่องค้างรุ่นเก่า
> **และ** กดแก้รายการขั้น · ทุกทางที่เขียน `processes` ผ่าน `saveProcessList()` ทางเดียว
> ซึ่งบรรทัดแรกคือด่านรหัส · เจ้าของเป็นคนเดียวที่รู้รหัส จึงเป็นคนเดียวที่ลบธงได้
> แลกกับการ redeploy ทุกเครื่องไม่คุ้ม และความเสี่ยงลดเองเมื่อเครื่องทยอยโหลดรุ่นใหม่
>
> **วิธีกันจริง:** กด Ctrl+F5 ก่อนเปิดแผงจัดการขั้น ดูให้เลขรุ่นมุมจอเป็น `2026-09-20.4` ขึ้นไป
> ⚠️ ด่านรหัสเป็น**ฝั่งหน้าจอล้วน** เซิร์ฟเวอร์ไม่ได้บังคับ — กันเพื่อนร่วมงานเผลอกดได้
> แต่ไม่ได้กันคนที่มี URL กับ token แล้วยิง `pushSettings` เอง (ตรงกับที่เขียนไว้เรื่องรหัสหัวหน้า)

---

## 2. Deploy

**push ขึ้น `main` = ขึ้น production ทันที** ผ่าน GitHub Pages
ไม่มี build ไม่มี test runner ไม่มี staging → https://nse-manufac.github.io/plan/

agent **ห้าม push ขึ้น `main` เด็ดขาด** ทุกการแก้ต้องเป็น branch + PR ให้เจ้าของอนุมัติ

---

## 3. Stack

- **JavaScript ล้วน ไม่มี framework** — จัดการ DOM เองด้วย `document.getElementById` + `innerHTML`
  (ต่างจาก repo `store` ที่ใช้ Vue — อย่าเอารูปแบบของอีกอันมาปน)
- **ExcelJS** และ **JSZip** ถูก **vendor ไว้ในไฟล์** ไม่ได้โหลดจาก CDN
  ExcelJS ใช้ **อ่าน** ไฟล์ที่ผู้ใช้เลือก และ **สร้าง** ไฟล์รายงานใหม่
  JSZip ใช้ **แก้ไฟล์ที่มีอยู่แล้วเฉพาะช่องที่ต้องแก้** โดยไม่แตะส่วนอื่นเลย
  > ⚠️ **ห้ามใช้ ExcelJS เขียนทับฟอร์มของบริษัท** — มันเขียนไฟล์ใหม่ทั้งไฟล์
  > ของที่มันไม่รู้จักจะหายเงียบ ๆ ทดสอบกับไฟล์จริงแล้วพบว่า Call In เสียสูตร
  > VLOOKUP ข้ามไฟล์ไป 698 สูตร และ FM-ST-07 เสีย `xl/drawings/` จนโลโก้ไม่ขึ้น
  (เดิมเป็น SheetJS เปลี่ยนเมื่อ 29 ส.ค. 2026 เพราะ community build ของ SheetJS
  เขียนเส้นขอบ สีพื้น ตัวหนา ลงไฟล์ไม่ได้ จึงกรอกฟอร์มของบริษัทไม่ได้เลย)
- ไม่มี npm ไม่มี bundler ไม่มี TypeScript ไม่มี `<script src>` ภายนอกเลย
  (`package.json` มีไว้ให้ชุดเทสเท่านั้น ตัวแอปยังไม่มี build step และไม่มี dependency ตอนรัน)
- แอปทำงานได้แม้ไม่มีอินเทอร์เน็ต — คุณสมบัตินี้ตั้งใจ ห้ามทำหาย

---

## 3.1 กำลังแยกไฟล์อยู่ — อ่านก่อนเพิ่มโค้ดใหม่

INVARIANTS F2 ฉบับ 19 ก.ย. 2026 อนุญาตให้แยกเป็นหลายไฟล์แล้ว งานนี้ทำทีละก้อน
**ระบบที่พนักงานใช้ไม่เปลี่ยนเลย** — ยังเก็บใน localStorage key เดิม และยังซิงค์ผ่าน Apps Script ตัวเดิม

โครงที่กำลังเดินไปหา:

```
plan/
├── production_plan_tracker.html   ชื่อเดิม (บุ๊กมาร์กของพนักงานต้องไม่พัง)
├── app.js       ต่อสายอย่างเดียว
├── core/        กฎล้วน — ห้ามแตะ DOM · localStorage · fetch
├── io/          storage.js (localStorage) · gs-api.js (Apps Script) · version.js
├── excel/       อ่านไฟล์แผน · อ่าน Call In · กรอก FM-ST-07 · ออกรายงาน
├── ui/          หน้าจอแยกตามแท็บ
└── lib/         ไลบรารีที่ vendor ไว้ — แตะไม่ได้ (F2)
```

### สองเฟส (เจ้าของเคาะ 23 ก.ย. 2026)

**เฟส 1 — สคริปต์ธรรมดา ย้ายอย่างเดียว** · ทุกไฟล์โหลดด้วย `<script src>` ธรรมดา **ยังไม่ใช่ ES module**
ก้อน 02 ถอดเปลือก IIFE ออกครั้งเดียว (ลบสองบรรทัด) หลังจากนั้นทุกไฟล์มองเห็นกันเหมือนตอนอยู่ไฟล์เดียว
จึงย้ายได้โดยไม่ต้องแก้บรรทัดไหนเลย และเครื่องพิสูจน์ได้ทุกตัวอักษร
> ⚠️ **ลำดับ `<script src>` คือลำดับการทำงาน** — โค้ดที่รันตอนโหลดหน้าต้องอยู่หลังไฟล์ที่มันเรียกใช้
> ตัวพิสูจน์ตรวจข้อนี้ไม่ได้ Playwright เป็นตัวจับ

**เฟส 2 — ES module** · ภายหลัง ไม่มีเส้นตาย พลิกทีละไฟล์ ไล่จากไฟล์ที่ *ใช้* คนอื่นก่อน ไฟล์ที่ *ถูกใช้* ทีหลัง
ใบละไฟล์ มีแค่บรรทัด `import`/`export` เพิ่ม · ตัวแปรที่ถูกเขียนทับจากหลายที่ (เช่น `state`) ข้ามโมดูลไม่ได้
ต้องแก้ตรรกะทีละตัวพร้อม Playwright — **ห้ามเอาเฟส 2 มาปนในใบของเฟส 1**
> ⚠️ **ไฟล์ยังต้องลงท้าย `.js` ห้ามเปลี่ยนเป็น `.mjs`** — ด่านเพดาน · ด่านบัมป์เลขรุ่น · ด่าน vendor
> และตัวพิสูจน์ นับเฉพาะ `*.html` `*.js` (`.github/scripts/app-files.sh`) ไฟล์ `.mjs` จะหลุดทุกด่านเงียบ ๆ

### ใครย้าย และผ่านด่านยังไง

> ⚠️ **งานย้ายโค้ดเป็นของ agent** — แก้เมื่อ 23 ก.ย. 2026
> ฉบับเดิมเขียนว่าเป็นของเจ้าของ ซึ่งตั้งอยู่บนสมมติฐานที่ไม่จริง: **เจ้าของไม่เขียนโค้ดเอง**
> แผนเดิมจึงไม่มีวันเกิด หรือไม่ก็ต้องหลบด่านแบบไม่มีใครตัดสินใจ ซึ่งแย่กว่า

PR ย้ายโค้ดเกินเพดาน 150 บรรทัดแน่นอน **ทางผ่านทางเดียว** คือครบสองข้อในรอบเดียวกัน
1. `.github/scripts/move-proof.mjs` พิสูจน์ได้ว่า ไม่มีโค้ดใหม่ · ไม่มีโค้ดหาย · ย้ายทั้งก้อนตรงทุกตัวอักษร
   และทั้งใบไม่แตะอะไรนอกไฟล์แอป เว้นแต่สามอย่างในกติกาข้อ 3 ข้างล่าง (ซึ่งด่านพิมพ์ชื่อไว้ให้อ่านด้วยตา)
2. เจ้าของติด label **`move-only`** ในรอบนั้น (กติกากันของค้างเดียวกับ `vendor-change`)

- **ห้ามหลบเพดานด้วยการตั้งชื่อสาขาให้ไม่ขึ้นต้น `claude/`** — ด่านมีข้อยกเว้นที่ถูกต้องให้แล้ว
- ก้อน `lib/` ยังชนกฎ "agent แตะ vendor ไม่ได้เด็ดขาด" ซึ่ง **ไม่ได้แก้** — เจ้าของเลือกกดเมิร์จข้ามด่านเองครั้งเดียว
  ด่านพิมพ์บอกว่าไลบรารีถูกย้ายตรงทุกไบต์ไหม เป็นข้อมูลประกอบการตัดสินใจ (ไม่เปลี่ยนคำตัดสิน)
- agent ยังทำงานแก้บั๊กบนโครงที่มีอยู่ ณ ตอนนั้นได้ตามปกติ ไม่ว่าจะแยกไปถึงไหนแล้ว

### ทำเป็นใบซ้อนกันทั้งชุด

base ของแต่ละใบคือสาขาของใบก่อนหน้า diff ของแต่ละใบจึงเป็นก้อนเดียว · workflow ทุกตัวรันกับทุก base แล้ว
- เมิร์จด้วย **"Create a merge commit"** ไล่จากใบล่างสุด แล้วเปลี่ยน base ใบถัดไปเป็น main (`gh pr edit <N> --base main`)
- **ห้าม squash ชุดนี้** — squash ทำให้ใบถัดไปเห็น diff ของใบก่อนซ้ำทั้งก้อน ต้อง rebase ใหม่ทุกใบ
- แก้ใบล่าง → rebase ใบบนทุกใบ → ผู้ตรวจรันใหม่ทุกใบที่อยู่บนมัน

### กติกาของทุก PR ที่ย้ายโค้ด

1. **ย้ายอย่างเดียว ห้ามแก้ตรรกะ** เห็นบั๊กระหว่างย้ายให้เปิด issue แยก
2. **ห้ามเหลือโค้ดสองชุด** ย้ายแล้วลบของเดิมในก้อนเดียวกัน (ตัวพิสูจน์จับทั้งก็อปซ้ำและลบทิ้ง)
3. **เพิ่มเทส node ของก้อน `core/` ใน PR เดียวกัน** ตั้งชื่อเทสอ้างชื่อกฎตรง ๆ เช่น `A1 — ...`
   เป็นไฟล์ **ใหม่** ใน `tests/` เท่านั้น — ใบย้ายอย่างเดียวแตะนอกไฟล์แอปได้แค่สามอย่าง:
   บรรทัดอนุญาตใน `.gitignore` · ไฟล์เทสใหม่ · `CLAUDE.md` · แก้/ลบเทสเดิม แก้ `.gs` หรือ `package.json` = ตัวพิสูจน์ตก
   (สคริปต์ `test:core` ใน `package.json` จึงต้องมาในใบ 00 ครั้งเดียว)
4. **Playwright ชุดเดิมต้องเขียว** นี่คือตาข่ายที่ยืนยันว่าพฤติกรรมไม่เปลี่ยน
5. หัว PR เขียนว่า **"ย้ายอย่างเดียว"** และเจ้าของติด label `move-only` ตอนจะเมิร์จ
6. **ไฟล์หรือโฟลเดอร์ใหม่ต้องเพิ่มใน `.gitignore`** — ไฟล์นั้นเป็นแบบปฏิเสธทุกอย่างก่อน
   ลืมแล้ว git ไม่ commit ไฟล์ใหม่ **เงียบ ๆ** (ตัวพิสูจน์จะรายงานว่าโค้ดหาย — เจอจริงตอนลองกับไฟล์จริง)
7. **เทสที่อ่านซอร์สของแอปต้องอ่านผ่านตัวช่วยที่รวม HTML กับทุกไฟล์ที่หน้าโหลด**
   ไม่งั้นเทสแบบ "ซอร์สต้อง *ไม่มี* X" จะเขียวเองทันทีที่โค้ดย้ายออกจาก HTML โดยไม่ได้ตรวจอะไร (ใบ 00)

ลำดับที่วางไว้: 00 เทสอ่านซอร์สผ่านตัวช่วย → 01 `lib/` → 02 ถอดเปลือก IIFE → 03 ย้ายทั้งบล็อกไป `app.js`
→ `core/dates` → `core/cumulative` → `core/status` → `core/fg` → `core/delivery` → `io/` → `excel/` → `ui/`

**ยังไม่มีไฟล์ไหนถูกแยกจริง ณ ตอนนี้** — ตารางใน §4 ยังใช้ได้ทั้งหมด

---
## 4. หาที่แก้ยังไงโดยไม่ต้องอ่านทั้งไฟล์

ไฟล์ยาว **5,324 บรรทัด** แต่ **~1,014 KB จาก ~1,409 KB เป็นโค้ด ExcelJS + JSZip ที่ vendor ไว้**
กระจุกอยู่ใน **5 บรรทัด** ช่วงกลางไฟล์ (บรรทัดละ 20,000–500,000 ตัวอักษร)

> ⚠️ **ห้ามอ่าน ห้ามแก้ ห้าม format บรรทัดเหล่านั้น** และห้ามอ่านทั้งไฟล์เข้า context
> วิธีเช็กว่าบรรทัดไหนเป็น vendor: ถ้าบรรทัดยาวเกิน 20,000 ตัวอักษร คือ vendor

โค้ดของแอปจริงแบ่งเป็นบล็อกด้วย section banner ค้นด้วย:

```
grep -n "^/\* -\{5,\}" production_plan_tracker.html
```

| Section banner | มีอะไร |
|---|---|
| `Basic helpers` | `normalizeDateOnly` `fmtDateTH` `addDaysISO` `daysBetween` `escapeHtml` `uid` `toast` |
| `Processes` / `รายการขั้นการผลิต` | `DEFAULT_PROCESSES` (ที่เดียวของรายการขั้น) → `PROCESSES` **`MAIN_PROCESSES`** `PREV_PROCESS` `ptext` `hasOrderAnomaly` · `ORDER_DATE_FIELDS` `RECORD_DATE_FIELDS` |
| `State` | `STORAGE_KEY` `defaultState` `loadState` `saveState` `migrateCorruptedDates` |
| `Google Sheets sync` | `gsApi` `cleanForPush` `doSync` และการ merge ตอน pull |
| `แก้ไฟล์ Excel เฉพาะช่องที่ต้องแก้` | ตัวช่วยแก้ XML ในไฟล์ .xlsx ผ่าน JSZip |
| `ทำให้ Excel คิดสูตรใหม่หลังเราแก้ช่องต้นทาง` | `clearCachedFormulaValues` `setFullCalcOnLoad` ← อ่านคอมเมนต์ก่อนแตะ |
| `ยอดคงคลัง FG` | `fgRows` `fgMoves` — คิดสดจากสมุด ไม่เก็บยอดไว้ที่ไหน |
| `Render: ยอดคงคลัง FG` | `renderFg` `renderFgCard` `fgDiffCell` |
| `ใบส่งสินค้า` | โครงข้อมูลใบส่งสินค้า · `deliveryQty` `canonicalPack` · และ `normalizeRowDates` ที่มานอนอยู่ตรงนี้ |
| `ใบส่งสินค้า: ข้อมูลและการคิดยอด` (ต่อ) | `deltaWipOf` `saveDeltaWip` `weekFromSheetName` — ยอดค้างที่ Delta บันทึกไว้ |
| `ใบส่งสินค้า: ข้อมูลและการคิดยอด` | `deliveryGroups` `savePacking` `saveAllocation` `resetPacking` `samePn` |
| `กรอกฟอร์ม Daily Call In` | `readCallInSheet` อ่านไฟล์ของ Delta · `planCallIn` เทียบกับยอดเรา |
| `Excel parsing` | อ่านไฟล์แผนงานจากลูกค้า |
| `Cumulative / deadline / status helpers` | `buildCumMap` `buildCumSplitMap` `computeDeadlines` `computeStatus` ← **หัวใจของ A1–A5** |
| `Render: Import tab` | หน้านำเข้า |
| `Render: Entry tab` | หน้าคีย์ยอดรายวัน · ปุ่มเลือกขั้นวาดจากรายการขั้นใน `renderProcessControls` |
| `Record editor` | แก้ไข/ยกเลิกรายการที่บันทึกแล้ว · `saveEntryValue` อยู่ในบล็อกนี้ |
| `Render: Dashboard tab` | ตารางหลัก + กราฟ |
| `Render: Data tab` / `Merge logic` | หน้าข้อมูล · `computeMergePlan` `showMergeModal` `applyMerge` |
| `Excel report export` | ออกรายงาน |
| `กรอกฟอร์มใบส่งสินค้า FM-ST-07` | `fillDeliveryForm` `setDataMerges` — เขียนลงฟอร์มจริง |
| `หน้าจอใบส่งสินค้า` | `renderDeliveryNote` `dnFilter` `bindDnInputs` `refreshDnGroup` |
| `ออกไฟล์ใบส่งสินค้า FM-ST-07` | ด่านกันออกใบตอนกล่อง/เศษไม่ตรงยอด |
| `หน้าจอ: กรอกฟอร์ม Daily Call In` | หน้าแสดงผลเทียบยอด — **อ่านอย่างเดียว ไม่เขียนไฟล์** |
| `Orchestration` / `Event bindings` / `Init` | ผูก event และเริ่มระบบ |

---

## 4.1 การอ่านไฟล์ใบรับ Order — ห้ามกลับไปใช้ตำแหน่งคอลัมน์ตายตัว

ไฟล์ของฝ่ายวางแผน **มีผังคอลัมน์มากกว่าหนึ่งแบบปนกันอยู่ในไฟล์เดียว**
(สำรวจไฟล์จริง 36 ชีต เมื่อ 4 ก.ย. 2026)

| | ใช้ในสัปดาห์ | `Open Q'ty` | คอลัมน์วันแผน |
|---|---|---|---|
| ผังปัจจุบัน | WK 15 เป็นต้นมา | คอลัมน์ L | M · N · O |
| ผังเก่า | WK 1–14 | **คอลัมน์ M** (L คือ `Order Q'ty`) | ไม่มี |

โค้ดเดิมอ่านตำแหน่งตายตัว ชีตเก่าจึงได้ยอดจากคนละช่อง และได้ "วันแผน"
ที่แปลงมาจากตัวเลขจำนวน โดยไม่มีอะไรฟ้อง

ตอนนี้ `findPlanCols()` หาคอลัมน์จาก**ข้อความหัวตาราง** ตาราง `PLAN_HEAD`
เป็นที่เดียวที่กำหนดว่าฟิลด์ไหนจับคู่กับหัวอะไร · ขาดคอลัมน์ใน `PLAN_REQUIRED` = **ฟ้อง ไม่ใช่เดา**

สามข้อที่ต้องรู้ก่อนแก้

- **หัวตารางจริงสะกดว่า `Winding/Trimmimg`** (พิมพ์ตก) — จับแบบหลวมเสมอ อย่าเทียบตรงตัว
- **ตัวสำรอง (`alt`) ต้องกวาดรอบสองเท่านั้น** ผังเก่ามี `Order Q'ty` อยู่ซ้ายมือของ `Open Q'ty`
  ถ้ากวาดรอบเดียว ตัวสำรองจะชิงคว้าช่องไปก่อนตัวหลัก แล้วได้ยอดผิดช่อง
- **คอลัมน์ C ไม่มีหัวตาราง** ในไฟล์จริง (หัวข้ามจาก `B=Code` ไป `D=OSC`)

### Sub-Name มาจากตัวอักษรในรหัส PO ไม่ใช่คอลัมน์ C

`subNameFromPo()` — รหัส PO มี `H` = **TUE-H** · มี `U` = **TUE-U**

เจ้าของสั่งเปลี่ยนเมื่อ 4 ก.ย. 2026 หลังพบว่าไฟล์จริงมี **89 แถวที่คอลัมน์ C
ขัดกับตัวอักษรใน PO** และยืนยันว่า **ตัวอักษรใน PO ถูก คอลัมน์ C พิมพ์ผิด**

> ⚠️ **มีทั้ง U และ H หรือไม่มีเลย = คืนค่าว่าง ห้ามเดาข้างใดข้างหนึ่ง**
> ใบส่งสินค้าแยกตาม Sub-Name เดาผิดแล้วของไปออกใบผิดหน่วยโดยไม่มีใครรู้
> หน้าตรวจก่อนนำเข้านับแถวที่ว่างขึ้นมาเตือน — ตรวจกับไฟล์จริงแล้วไม่มีสักแถว

`DLG-H` ที่เคยมีในไฟล์ (498 แถว ชีตสุดท้าย WK 9) **เจ้าของยืนยันว่าเลิกใช้แล้ว ไม่ต้องรองรับ**

## 5. โครงสร้างข้อมูล

เก็บใน localStorage 2 key:

| key | เก็บอะไร |
|---|---|
| `tue_order_tracker_v1` | `state` ทั้งก้อน |
| `tue_order_tracker_sync_v1` | การตั้งค่าเชื่อมต่อ Google Sheets |

**state**

```js
{
  version: 1,
  deviceName: '',
  deadlineOffsets: { winding:10, assembly:17, support:null, inspection:24, shipping:28 },  // ซิงค์ข้ามเครื่อง (null = ยังไม่กำหนด)
  processes: null,   // รายการขั้นที่ซิงค์มาครั้งล่าสุด [{id,label,short,hidden,...}] · null = ห้าขั้นตั้งต้น (ดู composeProcesses)
  chartPref:  { mode:'14', from:'', to:'' },                                 // เครื่องนี้เท่านั้น ห้ามซิงค์
  orders: [], records: [], deliveryNotes: [], deltaWip: [], importHistory: []
}
```

**record — ยอดที่คีย์รายวัน**

```js
{
  id: uid(),
  date,                       // YYYY-MM-DD (วันปฏิทินล้วน)
  orderId, process,           // process ∈ PROCESSES
  qty, note, deviceName,
  createdAt, updatedAt,       // ISO 8601 เต็ม
  voided: false,              // ยกเลิกแบบไม่ลบ
  _dirty: true                // ภายในเครื่องเท่านั้น ห้ามส่งขึ้นเซิร์ฟเวอร์
}
```

**กุญแจประจำตัวของ record คือ `orderId|process|date`** ไม่ใช่ `id` — ดู INVARIANTS A2

---

## 6. ขั้นตอนการทำงานที่ต้องทำตาม

1. **อ่าน INVARIANTS.md** แล้วระบุในหัว PR ว่าการแก้นี้ไปแตะหมวดไหนบ้าง (A–G)
2. **ทำซ้ำอาการก่อน** เขียนใน issue ว่าเกิดจากอะไร บรรทัดไหน ก่อนแก้
3. **แก้ให้เล็กที่สุด** แก้เฉพาะสาเหตุ ห้ามจัดระเบียบโค้ดรอบข้างมาด้วย
   PR ที่ diff เกิน ~80 บรรทัดจะรีวิวตอนพักเที่ยงไม่ทัน ถ้าจำเป็นต้องใหญ่กว่านั้นให้บอกเหตุผลไว้ในหัว PR
4. **ตรวจว่า diff ไม่ไปโดนบรรทัด vendor** — ถ้า diff ใหญ่ผิดปกติ แปลว่าเผลอแตะ ExcelJS เข้าแล้ว
5. **หนึ่ง issue = หนึ่ง PR**
6. **เขียนสรุปเป็นภาษาไทย** หัว PR ต้องมี: อาการคืออะไร · เกิดจากอะไร · แก้ยังไง · ต้องทดสอบอะไร
7. **ห้าม push `main` · ห้าม merge เอง · ห้ามแก้ `.github/workflows/` เพื่อข้ามการตรวจ**

---

## 7. หยุดแล้วถามเจ้าของก่อน เมื่อเจอกรณีเหล่านี้

- ต้องเปลี่ยนโครงสร้าง `state` หรือ key ของ localStorage (INVARIANTS E1/E2)
- ต้องเพิ่ม / ลบ / สลับขั้นตอนการผลิต (INVARIANTS A3) — รวมถึงแก้ `DEFAULT_PROCESSES` หรือเปลี่ยน `id` ของขั้น
- ต้องเปลี่ยนกุญแจ `orderId|process|date` (INVARIANTS A2)
- ต้องเปลี่ยนวิธี sync หรือ contract กับ Apps Script
  (ผู้ใช้ต้องไป re-deploy Apps Script เอง = ต้องแจ้งล่วงหน้า)
- ต้องเพิ่ม dependency หรือเปลี่ยนวิธีโหลด ExcelJS
- แก้ปัญหาไม่ได้โดยไม่ละเมิด INVARIANTS ข้อใดข้อหนึ่ง
- เรื่องที่แจ้งมาไม่ใช่บั๊กของโค้ด แต่เป็นไฟล์นำเข้าผิด หรือผู้ใช้ใช้ผิดวิธี
  → ตอบกลับผู้แจ้งเป็นภาษาไทย แล้วปิด issue ไม่ต้องเปิด PR

---

## 8. ทดสอบยังไง

### 8.1 Smoke test อัตโนมัติ

รันเองทุก PR ผ่าน `.github/workflows/smoke.yml` — **PR ที่เทสแดงห้ามเสนอให้เจ้าของอนุมัติ**

```bash
npm install && npx playwright install chromium && npm test
```

ชื่อเทสอ้างข้อกฎใน `INVARIANTS.md` ตรง ๆ (เช่น `A2 — คีย์ยอดซ้ำ ... ต้องทับของเดิม`)
เทสแดงข้อไหน = ละเมิดกฎข้อนั้น ไปอ่านกฎก่อน อย่าเพิ่งไปแก้เทส

มีเทสหนึ่งข้อที่ **ตัดเน็ตทั้งหมดแล้วตรวจว่าแอปยังใช้งานได้** — เป็นตัวกันไม่ให้ใครเผลอ
เปลี่ยน ExcelJS ที่ vendor ไว้ไปเป็น CDN (INVARIANTS F2)

> `package.json` กับ `tests/` เป็น**เครื่องมือตอนพัฒนาเท่านั้น**
> ตัวแอปยังเป็นไฟล์เดียวไม่มี build ไม่มี dependency เหมือนเดิม

**เทสขับผ่าน DOM** เพราะโค้ดแอปห่ออยู่ใน IIFE จึงไม่มีฟังก์ชันไหนหลุดออกมาที่ global
วิธีตรวจผลคืออ่าน `localStorage` และข้อความในตาราง ไม่ใช่เรียกฟังก์ชันตรง ๆ

### 8.2 ทดสอบด้วยมือเพิ่ม (สำหรับสิ่งที่เทสยังไม่ครอบคลุม)

ชุดเทสตอนนี้มี **313 ข้อใน 14 ไฟล์** ครอบการคีย์ยอด · audit trail · วันที่ · ออฟไลน์ ·
การนำเข้า/ออกไฟล์ Excel · ใบส่งสินค้า · การเทียบยอดกับไฟล์ Call In

**สิ่งที่เทสยังตรวจแทนตาไม่ได้ และต้องทำด้วยมือทุกครั้งที่แตะ**

- **เปิดไฟล์ Excel ที่โปรแกรมออกมาด้วย Excel จริง** — เทียบ XML ได้ แต่บอกไม่ได้ว่าคนเปิดแล้วเห็นอะไร
  ช่องที่ merge · โลโก้ · เส้นขอบ · และยอดรวมท้ายตารางว่าไม่นับซ้ำ
- **กราฟบน Dashboard**
- **เครื่องที่รับข้อมูลมาจากการซิงค์** — ชนิดข้อมูลของ P/N ต่างจากเครื่องที่นำเข้าไฟล์เอง
  บั๊กที่เจอ 1 ก.ย. 2026 โผล่เฉพาะบนเครื่องแบบนั้น

ถ้า PR แตะส่วนนั้น ให้ทดสอบด้วยมือแล้วเขียนผลใน PR:

1. เปิด `production_plan_tracker.html` ในเบราว์เซอร์ ไม่ต้องตั้งค่า sync
2. นำเข้าไฟล์แผนงานตัวอย่าง
3. **เส้นทางที่ต้องผ่านทุกครั้ง:**
   - คีย์ยอด winding 1 ช่อง → Dashboard อัปเดตยอดสะสมถูกต้อง
   - คีย์ยอดวันเดิม order เดิม ขั้นเดิมซ้ำ → ทับค่าเดิม ไม่เกิดแถวใหม่
   - เคลียร์ช่องให้ว่าง → ค่าเดิมต้องยังอยู่ (ไม่ถูกลบ)
   - พิมพ์ `0` ทับ → ยอดเป็น 0
   - ยกเลิกรายการจาก Record editor → หายจากยอดสะสม แต่ยังอยู่ในรายการ
   - ปรับ `deadlineOffsets` ใน Settings → สีเตือนในตารางเปลี่ยนตาม
   - export Excel → เปิดไฟล์ได้ ตัวเลขตรงกับหน้าจอ
   - refresh หน้า → ข้อมูลทั้งหมดยังอยู่
4. **ทดสอบแบบไม่มีเน็ต** ตัดเน็ตแล้วเปิดไฟล์ ต้องใช้งานได้ครบ
5. เปิด DevTools Console ต้องไม่มี error

---

## 9. คำศัพท์

| คำ | หมายถึง |
|---|---|
| order | ใบสั่งซื้อ 1 ใบ พร้อมยอดที่ต้องผลิต (`orderQty`) |
| record | ยอดที่คีย์ 1 ครั้ง = order + ขั้นตอน + วันเดียว |
| winding | พันขดลวด / ตัดแต่ง — ขั้นแรก |
| assembly | ประกอบ |
| support | ขั้นย่อยระหว่างประกอบกับตรวจสอบ (ยังไม่มีชื่อไทย) |
| inspection | ตรวจสอบ |
| shipping | ส่งของ — ขั้นสุดท้าย ทยอยส่งได้ · ยอดมาจากหน้าใบส่งสินค้า |
| `deadlineOffsets` | จำนวนวันนับจาก `orderDate` ที่แต่ละขั้นต้องเสร็จ |
| ยอดสะสม (cum) | ยอดรวมทุกวันของขั้นนั้นในใบสั่งซื้อนั้น |
| ทันกำหนด / เกินกำหนด | แยกยอดสะสมตามว่าคีย์ก่อนหรือหลัง deadline ของขั้นนั้น |
