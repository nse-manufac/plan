# เครื่องของเจ้าของเป็น Windows ที่แปลง LF เป็น CRLF ให้อัตโนมัติ
# สคริปต์เชลล์ที่มี CR ติดไป จะรันบน runner ของ CI ไม่ได้เลย (bash: $'\r': command not found)
# บังคับให้ไฟล์กลุ่มนี้เก็บและ checkout เป็น LF เสมอ ไม่ว่าจะอยู่เครื่องไหน
*.sh text eol=lf
