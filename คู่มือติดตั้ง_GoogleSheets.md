# คู่มือติดตั้ง Google Sheets — ให้พนักงานกรอกยอด แล้วดู Dashboard จากเครื่องไหนก็ได้

ทำครั้งเดียว ใช้เวลาประมาณ 15 นาที · **ไม่ต้องเขียนโค้ดเอง แค่คัดลอกวาง**

> **เคย deploy สคริปต์นี้ไปแล้ว?** เวอร์ชันล่าสุดมี 2 การแก้ไขสำคัญ:
> 1. ฟีเจอร์ลบ/กู้คืนรายการที่บันทึกผิด (คอลัมน์ `voided`/`batchId` ใหม่ในชีต `Records`)
> 2. **แก้บั๊กวันที่เพี้ยน** — Google Sheets เคยแปลงคอลัมน์วันที่ (Order Date เป็นต้น) เป็นเซลล์ชนิดวันที่เอง
>    ทำให้ดึงกลับมาแสดงผลเพี้ยนและ deadline คำนวณไม่ได้ (`NaN`) เวอร์ชันนี้กันไม่ให้เกิดซ้ำ
>
> **คัดลอกโค้ดจาก `google-apps-script.gs` ไปวางทับของเดิมอีกครั้ง แล้ว Deploy → Manage deployments → ✏️ → Version: New version → Deploy** (ขั้นตอนที่ 2-3 ด้านล่าง) คอลัมน์ใหม่จะถูกเติมให้อัตโนมัติ ข้อมูลเดิมไม่หาย — ถ้ายังไม่ redeploy การลบ/กู้คืนจะยังทำงานได้ในเครื่องนั้นเครื่องเดียว แค่ไม่ซิงค์ไปเครื่องอื่น
>
> **หลัง redeploy ให้รีเฟรชหน้า Google Sheet แล้วเข้าเมนู ระบบติดตามแผนงาน → "ซ่อมรูปแบบวันที่ (ทำครั้งเดียวหลังอัปเดต)" หนึ่งครั้ง** เพื่อซ่อมข้อมูลวันที่ที่เพี้ยนอยู่แล้วในชีตให้กลับมาถูกต้องถาวร (ปลอดภัย รันซ้ำได้) — ฝั่งโปรแกรมเองก็ซ่อมข้อมูลที่เพี้ยนในเครื่องให้อัตโนมัติอยู่แล้วตั้งแต่เปิดไฟล์เวอร์ชันนี้ครั้งแรก แต่ถ้าไม่ซ่อมที่ต้นทางในชีตด้วย ข้อมูลเก่าจะเพี้ยนกลับมาให้ซ่อมใหม่ทุกรอบซิงค์

---

## ทำไมต้องทำ

ตอนนี้โปรแกรมเก็บข้อมูลไว้ในเบราว์เซอร์ของเครื่องที่คีย์เท่านั้น — เครื่องอื่นมองไม่เห็น
พอต่อ Google Sheets แล้วจะได้:

- พนักงานกรอกยอดที่เครื่องหน้างาน แล้วเปิดดู Dashboard จากเครื่องผู้จัดการ/สำนักงานได้ทันที ไม่ต้องส่งไฟล์ไปมา
- หลายคนกรอกพร้อมกันได้ เห็นยอดตรงกันทุกเครื่อง
- ข้อมูลไม่หายถ้าเครื่องพัง หรือเผลอล้างประวัติเบราว์เซอร์
- **เน็ตหลุดก็ยังกรอกงานต่อได้** ระบบส่งขึ้นให้เองเมื่อเน็ตกลับมา

---

## ขั้นตอน

### 1 · สร้าง Google Sheet ใหม่

1. เข้า [sheets.google.com](https://sheets.google.com) กด **+ Blank**
2. ตั้งชื่อไฟล์ เช่น `ระบบติดตามแผนงาน Production — ฐานข้อมูล`

> ใช้บัญชี Google ของบริษัท ไม่ใช่บัญชีส่วนตัว เพราะข้อมูลนี้เป็นของบริษัท

### 2 · วางสคริปต์

1. ในหน้า Sheet เลือกเมนู **ส่วนขยาย (Extensions) → Apps Script**
2. จะเปิดแท็บใหม่ มีโค้ดตัวอย่าง `function myFunction() {}` — **ลบทิ้งให้หมด**
3. เปิดไฟล์ `google-apps-script.gs` (อยู่โฟลเดอร์เดียวกับไฟล์นี้) คัดลอกทั้งไฟล์มาวางแทน
4. **แก้บรรทัดที่ 10** ตั้งรหัสผ่านของคุณเอง

   ```javascript
   const TOKEN = 'CHANGE-ME-1234';        // ← เปลี่ยนบรรทัดนี้
   const TOKEN = 'production-tue-2026-p9x'; // ← เป็นอะไรก็ได้ที่เดายาก
   ```

   จดรหัสนี้ไว้ เดี๋ยวต้องใช้ในขั้นตอนที่ 5

5. กดไอคอน **บันทึก** (💾) แล้วตั้งชื่อโปรเจกต์

### 3 · Deploy เป็น Web App

1. มุมขวาบนกด **Deploy → New deployment**
2. กดไอคอนเฟือง ⚙️ ข้าง "Select type" เลือก **Web app**
3. ตั้งค่าดังนี้ — **สำคัญมาก ถ้าตั้งผิดจะเชื่อมต่อไม่ได้**

   | ช่อง | ต้องเลือก |
   |---|---|
   | Description | `ProductionTrackerAPI` (อะไรก็ได้) |
   | **Execute as** | **Me (อีเมลคุณ)** |
   | **Who has access** | **Anyone** |

   > "Anyone" ฟังดูน่ากลัว แต่คนที่ไม่มี TOKEN จะเรียกใช้ไม่ได้เลย
   > ถ้าเลือก "Anyone with Google account" โปรแกรมจะเชื่อมต่อไม่ได้ เพราะติดหน้าล็อกอิน

4. กด **Deploy**
5. Google จะขออนุญาต → **Authorize access** → เลือกบัญชี →
   ขึ้นคำเตือน "Google hasn't verified this app" ให้กด **Advanced → Go to ... (unsafe)** → **Allow**

   > คำเตือนนี้ปกติ เพราะเป็นสคริปต์ที่คุณเขียนเอง ไม่ได้ส่งให้ Google ตรวจ

6. จะได้ URL หน้าตาแบบนี้ — **คัดลอกเก็บไว้**

   ```
   https://script.google.com/macros/s/AKfycbx......../exec
   ```

   ต้องลงท้ายด้วย `/exec` เท่านั้น (ถ้าลงท้าย `/dev` แปลว่าคัดลอกผิดอัน)

### 4 · สร้างชีตที่จำเป็น

กลับไปที่หน้า Google Sheet → **รีเฟรชหน้า** → จะมีเมนูใหม่ชื่อ **ระบบติดตามแผนงาน** โผล่ขึ้นมา

เลือก **ระบบติดตามแผนงาน → สร้างชีตที่จำเป็นทั้งหมด**

จะได้ 3 ชีต: `Orders` (บรรทัดแผนงานที่นำเข้าจาก Excel) `Records` (ยอดบันทึกรายวันทุก process) `Meta` (ค่า deadline ที่ตั้งไว้)

### 5 · ตั้งค่าในโปรแกรม

1. เปิด `production_plan_tracker.html` → แท็บ **ข้อมูล & แชร์**
2. เลื่อนลงมาที่กล่อง **"เชื่อมต่อ Google Sheets"**

   | ช่อง | ใส่อะไร |
   |---|---|
   | URL ของ Web App | URL จากขั้นตอนที่ 3 |
   | รหัสผ่าน (TOKEN) | รหัสที่ตั้งไว้ในขั้นตอนที่ 2 |

   (ชื่อเครื่องนี้ใช้ช่อง "ชื่อเครื่อง/ผู้บันทึก" ที่อยู่ด้านบนกล่องนี้ในแท็บเดียวกัน)

3. กด **ทดสอบการเชื่อมต่อ**
   ขึ้น "เชื่อมต่อได้ · ..." = เรียบร้อย
4. กด **เชื่อมต่อและดึงข้อมูลลงมา**

### 6 · นำเข้าแผนงานจากเครื่องแรก

ที่**เครื่องแรก**: ไปแท็บ **นำเข้าแผน** เปิดไฟล์ Excel แผนงานตามปกติ — พอกดยืนยันนำเข้า ระบบจะส่งขึ้น Google Sheets ให้เองอัตโนมัติ (ไม่ต้องกดอะไรเพิ่ม)

### 7 · ตั้งค่าเครื่องที่เหลือ

**เครื่องอื่นไม่ต้องมีไฟล์ Excel เลย** เลือกวิธีใดวิธีหนึ่ง

#### วิธีเร็วที่สุด — แจกลิงก์ตั้งค่า

1. ที่เครื่องแรก: แท็บ **ข้อมูล & แชร์** → กด **คัดลอกลิงก์ตั้งค่าให้เครื่องอื่น**
2. ส่งลิงก์ให้เครื่องอื่นทางแชทภายใน
3. เครื่องอื่นเปิดลิงก์ → **ตั้งค่าและดึงข้อมูลให้เองทันที** ไม่ต้องกรอกอะไรเลย

> ลิงก์มีรหัสผ่านอยู่ข้างใน ส่งเฉพาะในกลุ่มภายในเท่านั้น
> ระบบจะลบรหัสออกจากแถบที่อยู่ให้อัตโนมัติหลังเปิด

#### วิธีกรอกเอง

เปิด `production_plan_tracker.html` → แท็บ **ข้อมูล & แชร์** → กรอก URL + TOKEN → กด **เชื่อมต่อและดึงข้อมูลลงมา**

เสร็จแล้ว — ทุกเครื่องเห็น Dashboard ตรงกัน พนักงานหน้างานกรอกยอดรายวันที่เครื่องเดียว ผู้จัดการเปิดดู Dashboard จากเครื่องไหนก็ได้ ไม่ต้องส่งไฟล์ให้กัน

---

## 💡 แนะนำอย่างยิ่ง: เอาไฟล์ HTML ขึ้น GitHub Pages

ถ้าเอา `production_plan_tracker.html` ขึ้น GitHub Pages (แบบเดียวกับที่เคยทำกับโปรแกรมอื่น) จะได้:

- **หน้างานเปิดจากลิงก์ได้เลย ไม่ต้องมีไฟล์ในเครื่องสักไฟล์เดียว**
- แก้โปรแกรมทีเดียว ทุกเครื่องได้เวอร์ชันใหม่พร้อมกัน ไม่ต้องไล่ก๊อปไฟล์
- ไม่ต้องเจอปัญหาการเปิดจาก `file://` ซึ่งบางเบราว์เซอร์บล็อกการเชื่อมต่อออกนอกเครื่อง

แล้วแจกเป็นลิงก์เดียวที่มีค่าตั้งค่าติดไปด้วย:

```
https://ชื่อผู้ใช้.github.io/ชื่อโปรเจกต์/production_plan_tracker.html#gsurl=...&gstoken=...
```

บุ๊กมาร์กไว้ที่เครื่องหน้างาน เปิดปุ๊บใช้ได้เลย

---

## วิธีดูว่าซิงค์ปกติไหม

ดูป้ายกลม ๆ มุมขวาบนแถบสีน้ำเงิน **กดที่ป้ายเพื่อสั่งซิงค์ทันที**

| สี | ข้อความ | แปลว่า |
|---|---|---|
| 🟢 เขียว | ซิงค์แล้ว | ปกติ ข้อมูลตรงกับเซิร์ฟเวอร์ |
| 🟡 เหลือง | รอส่ง N / กำลังซิงค์ | กำลังทำงาน รออีกนิด |
| 🔴 แดง | เชื่อมต่อไม่ได้ · ค้าง N | เน็ตมีปัญหา — **กรอกต่อได้ตามปกติ** ระบบส่งให้เองทีหลัง |
| ⚪ เทา | ยังไม่เชื่อมต่อ | ยังไม่ได้ตั้งค่า URL |

---

## ปัญหาที่เจอบ่อย

| อาการ | สาเหตุและวิธีแก้ |
|---|---|
| กดทดสอบแล้วขึ้น **token ไม่ถูกต้อง** | TOKEN ในโปรแกรมไม่ตรงกับในสคริปต์ — เช็คช่องว่างหน้า-หลังด้วย |
| ขึ้น **HTTP 401 / 403** | ตอน Deploy ตั้ง "Who has access" ไม่ได้เป็น **Anyone** |
| ขึ้น **Failed to fetch** | URL ผิด (ต้องลงท้าย `/exec`) หรือเน็ตหลุดจริง |
| แก้โค้ดแล้วไม่มีผล | ต้อง **Deploy → Manage deployments → ✏️ → Version: New version → Deploy** ทุกครั้ง |
| ไม่เห็นเมนู "ระบบติดตามแผนงาน" | รีเฟรชหน้า Google Sheet |
| เครื่องหนึ่งเห็นข้อมูลไม่ครบ | แท็บข้อมูล & แชร์ → กด **ดึงข้อมูลใหม่ทั้งหมด** |
| Order Date เพี้ยน / Deadline ขึ้น `NaN` | สคริปต์ยังเป็นเวอร์ชันเก่าที่ Sheets แปลงคอลัมน์วันที่เป็นเซลล์วันที่เอง — วางโค้ดใหม่ทับ + redeploy แล้วรันเมนู **"ซ่อมรูปแบบวันที่"** (ดู callout ด้านบนสุด) |

---

## เรื่องความปลอดภัยที่ควรรู้

- **TOKEN คือกุญแจดอกเดียวที่กันคนนอก** ห้ามส่งในไลน์กลุ่มที่มีคนนอกบริษัท
- ถ้าสงสัยว่ารหัสรั่ว: แก้ `TOKEN` ในสคริปต์ → Deploy ใหม่ → แจ้งทุกเครื่องให้เปลี่ยนตาม
- Google Sheet ตัวนี้อย่าแชร์เป็น "Anyone with the link can edit"
- **ส่งออกสำรอง (.json) เก็บไว้ทุกสิ้นเดือน** แม้จะต่อ Google Sheets แล้ว (ปุ่ม Export ไฟล์ข้อมูล อยู่แท็บเดียวกัน)

---

## ข้อจำกัดที่ต้องรู้

| เรื่อง | รายละเอียด |
|---|---|
| ความเร็ว | ซิงค์อัตโนมัติทุก 20 วินาที · กดบันทึกยอดแล้วส่งขึ้นทันที |
| การลบยอดที่กรอกผิด | ระบบไม่ลบบันทึกด้วยการเคลียร์ช่องว่าง (กันข้อมูลย้อนแย้งข้ามเครื่อง) — ถ้ากรอกผิด ให้**พิมพ์ 0 ทับ**แทนการลบ |
| โควตา Apps Script | บัญชีฟรีเรียกได้ ~20,000 ครั้ง/วัน — ใช้งานปกติหลายเครื่องทั้งวันยังเหลือเยอะ |
| การชนกัน | 2 เครื่องแก้ยอดวันเดียวกัน/process เดียวกัน/PO เดียวกันพร้อมกัน → เครื่องที่กดบันทึกทีหลังชนะ |

---

## ทดสอบก่อนใช้จริง

ก่อนให้พนักงานใช้จริง แนะนำให้ลองด้วยตัวเองก่อน:

1. เปิดโปรแกรมบน 2 เครื่อง (หรือ 2 หน้าต่างเบราว์เซอร์คนละโปรไฟล์)
2. เครื่อง A นำเข้าแผนงาน + กรอกยอด Winding 1 รายการ
3. เครื่อง B กดป้ายซิงค์ (หรือรอ 20 วินาที) → ต้องเห็นแผนงานและยอดของ A ใน Dashboard
4. เครื่อง B กรอกยอด Assembly ต่อ → เครื่อง A ซิงค์ → WIP Dashboard ต้องตรงกันทั้งสองเครื่อง
5. ปิด Wi-Fi แล้วกรอกต่อ → ต้องกรอกได้ ป้ายขึ้นแดง → เปิด Wi-Fi → ต้องส่งขึ้นเอง

ถ้าครบ 5 ข้อนี้แปลว่าพร้อมใช้งานจริง
